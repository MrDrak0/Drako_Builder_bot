# Discord bot token from https://discord.com/developers/applications
# Replace YOUR_BOT_TOKEN_HERE with your real token — never commit the real token.
DISCORD_TOKEN=YOUR_BOT_TOKEN_HERE

# Optional: set to a test server ID for instant command registration there.
# Leave empty to register commands globally (can take up to 1 hour to propagate).
GUILD_ID=